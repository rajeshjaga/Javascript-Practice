console.clear();//clears the console for nodemon to watch the files


//datatype conversion from string to number
var age = 21;
console.log(age);
age = "Twenty-One";
console.log(age);

//datatype conversion from number to string
let number = 5;
console.log(number);
number = "number five";
console.log(number);

//datatype conversion from objects to arrays
let objects = {
    key: 'value'
}
console.log(objects);
objects = ['this is no more object', 'its an array'];
console.log(objects);


//conversion from boolean to string
console.log(typeof (String(true)))

//conversion from string to number using parseINT()
var stringToNumber = '10';
stringToNumber = parseInt(stringToNumber);
console.log(typeof (stringToNumber))
//conversion from string to number using parseINT()
var stringToFloat = '10.22222222';
stringToFloat = parseFloat(stringToFloat);
console.log(typeof (stringToFloat))

//datatype conversion

